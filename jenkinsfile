pipeline {
    agent any

    stages {
        stage('Get Maven Version') {
            steps {
                    sh "mvn --version"
            }
    }
    stage("mvn clean install")
    {
        steps{
            sh "mvn clean test"
        }
    }
    }
}
